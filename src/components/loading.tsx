import { ActivityIndicator } from "react-native";

export function Loading() {
    return (
        <ActivityIndicator className="flex-1 bg-slate-950 items-center justify-center text-slate-400"/>
    )
}